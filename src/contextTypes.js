import PropTypes from 'prop-types';

/* eslint-disable import/prefer-default-export */
export const reactReduxSSRContextTypes = PropTypes.shape({
  loadParams: PropTypes.object,
}).isRequired;
/* eslint-enable import/prefer-default-export */
